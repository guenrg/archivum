name: Deploy Web App

on:
  push:
    branches:
      - main
    paths:
      - "web/**"
      - "shared/**"
      - ".github/workflows/deploy-web.yml"
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # TODO: Add deployment steps for your web app host
      # (e.g., Azure App Service, Docker, Railway, etc.)
